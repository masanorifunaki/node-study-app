extends layout

block content
  form(action="/", method="post")
    span タイトル
    input(type="text" name="title" required)
    br
    br
    button(type="submit") ボート作成
    ul
      each boardItem in boardList
        li
          div
            p #{boardItem.title}
            p #{boardItem.created_at}



